# Cron Job: Daily Meta Ads Überblick

**Job ID:** 459977e365b1
**Run Time:** 2026-05-24 14:49:51
**Schedule:** 0 8 * * *

## Prompt

[IMPORTANT: The user has invoked the "lenaa-ads-performance-operator" skill, indicating they want you to follow its instructions. The full skill content is loaded below.]

---
name: lenaa-ads-performance-operator
description: Daily/recurring Meta Ads performance operator for lenaa. Uses the lenaa_ads MCP tools as source of truth, analyzes active ads, identifies winners/losers, and recommends the next sensible marketing step.
version: 1.0.0
author: Hermes Agent
license: MIT
metadata:
  hermes:
    tags: [lenaa, meta-ads, marketing, cron, mcp, performance]
---

# lenaa Ads Performance Operator

Use this skill for recurring or one-off Meta Ads performance checks for lenaa.

## Core Rules

1. **MCP data is the source of truth.** Never invent ad performance. Only report metrics retrieved via the lenaa Ads MCP tools or explicitly provided by Sahajit.
2. **Read-only by default.** Do not pause ads, change budgets, create campaigns, or edit creatives unless Sahajit explicitly approves a concrete action.
3. **Focus on active ads.** Prioritize ads with `status` or `effective_status` = `ACTIVE`. Mention paused ads only if useful context.
4. **Keep reports short and operator-style.** Sahajit prefers concise Telegram-ready summaries.
5. **Beauty-salon context matters.** Interpret creatives through lenaa's promise: fewer missed calls, less interruption during treatments, more booked appointments, calmer salon operations.

## MCP Tools

Available Hermes tool names may be prefixed as `mcp_lenaa_ads_...` in live sessions. Core logical tools:

- `ads_health` — service status and Meta config.
- `ads_summary(range)` — account overview: campaigns, ads, totals, creatives and insights.
- `ads_ads_list(limit, after)` — all ads including creative text.
- `ads_ad_get(id)` — full ad view: ad, creative, ad set, insights for 7d and 30d.
- `ads_ad_insights(id, range)` — individual ad performance.
- `ads_campaigns_list(limit, after)` / `ads_campaign_insights(id, range)` — campaign context.
- `ads_adsets_list(limit, after)` / `ads_adset_insights(id, range)` — ad set context and targeting.
- `ads_creative_get(id)` — creative details.

## Standard Analysis Flow

For daily overview:

1. Call `ads_health`. If not ok or `metaConfigured` is false, report the issue and stop.
2. Call `ads_summary` for `7d`.
3. Identify active ads from the returned ads list.
4. For each active ad, call `ads_ad_insights(id, range='7d')` if ad-level insights are needed and available.
5. Optionally call `ads_ad_get(id)` for the top 1-3 ads to inspect creative text and context.
6. Rank active ads by practical performance:
   - Primary: leads/conversions if present.
   - If no lead metric is present: CTR, CPC, spend, clicks, impressions, reach.
   - Treat very low spend/impressions as insufficient data.
7. Compare against account totals and previous/30d context if available.
8. Produce a short recommendation: scale, keep learning, refresh creative, duplicate winner, test new hook, or reduce attention on weak ad.

## KPI Interpretation

Use these default heuristics unless better account-specific benchmarks exist:

- CTR > 2%: good attention signal.
- CTR < 1%: weak hook/creative signal.
- CPC rising with weak CTR: creative likely not resonating.
- High CTR but low leads: landing page/form/offer mismatch likely.
- Good performance with meaningful spend: candidate to scale or duplicate.
- Good performance with tiny spend: monitor, don't overreact.
- Weak performance with enough spend: propose new creative angle before budget changes.

## Daily Telegram Report Format

Use this structure:

```text
## Meta Ads Überblick — letzte 7 Tage

Status: ✅/⚠️
Spend: X € · Clicks: X · Impressions: X · CTR: X% · CPC: X €

Beste aktive Ad:
- Name: ...
- Warum: ...
- Signal: ...

Weitere Beobachtungen:
- ...
- ...

Nächster sinnvoller Schritt:
➡️ ...

Keine Änderungen vorgenommen.
```

Keep it concise. Avoid long raw dumps. Include only the IDs if needed for actionability.

## No-Hallucination Fallbacks

If data is missing:

- Say exactly what is missing.
- Do not infer leads or conversions from clicks.
- If only account-level insights exist, say ranking is based on available signals, not confirmed leads.
- If a tool fails, include the failing tool and the error in one short line.

## Suggested Next-Step Patterns

- Winner with clear creative angle: "Neue Variante mit gleichem Hook bauen, anderer Einstieg/Visual."
- High interruption-pain creative works: "Mehr Creatives rund um 'Telefon klingelt während Behandlung' testen."
- Calculator/lost-revenue angle works: "Lead-Magnet stärker testen, CTA klarer machen."
- Low CTR: "Hook in den ersten 2 Sekunden erneuern; weniger erklären, schneller Schmerz zeigen."
- High CTR but weak lead result: "Offer/Form prüfen; 14 Tage kostenlos + 200€ Garantie prominenter testen."

The user has provided the following instruction alongside the skill invocation: [IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Du bist der tägliche Meta-Ads-Performance-Operator für lenaa. Erstelle einen kurzen Telegram-tauglichen Überblick über die letzten 7 Tage.

Pflichtablauf:
1. Nutze die lenaa Ads MCP Tools als einzige Datenquelle. Starte mit `mcp_lenaa_ads_ads_health`.
2. Wenn Health nicht ok oder Meta nicht konfiguriert ist: melde kurz den Fehler und stoppe.
3. Hole `mcp_lenaa_ads_ads_summary` mit range=`7d`.
4. Betrachte vor allem aktive Ads (`status` oder `effective_status` = `ACTIVE`).
5. Wenn möglich, hole für aktive Ads zusätzlich `mcp_lenaa_ads_ads_ad_insights` mit range=`7d` oder `mcp_lenaa_ads_ads_ad_get`, um Performance und Creative-Kontext zu prüfen.
6. Bestimme die beste aktive Ad anhand verfügbarer echter Daten. Priorität: Leads/Conversions, falls vorhanden; sonst CTR, CPC, Clicks, Spend, Impressions/Reach. Wenn ad-level Performance nicht verfügbar ist, sage klar, dass die Einschätzung auf den verfügbaren Signalen basiert.
7. Gib genau einen nächsten sinnvollen Schritt aus. Keine Änderungen an Kampagnen vornehmen.

Output-Format:
## Meta Ads Überblick — letzte 7 Tage

Status: ✅/⚠️
Spend: X € · Clicks: X · Impressions: X · CTR: X% · CPC: X €

Beste aktive Ad:
- Name: ...
- Warum: ...
- Signal: ...

Weitere Beobachtungen:
- ...
- ...

Nächster sinnvoller Schritt:
➡️ ...

Keine Änderungen vorgenommen.

Halte es kurz, konkret und business-nah für Sahajit. Keine Rohdaten-Dumps, keine Halluzinationen.

## Response

## Meta Ads Überblick — letzte 7 Tage

Status: ✅  
Spend: 139.74 € · Clicks: 82 · Impressions: 2929 · CTR: 2.80% · CPC: 1.70 €

Beste aktive Ad:
- Name: AI - Flow
- Warum: stärkstes echtes Lead-Signal unter den aktiven Ads
- Signal: 2 Leads · 59 Klicks · CTR 3.48% · CPC 1.27 € · CPL 37.48 €

Weitere Beobachtungen:
- Insgesamt wurden 3 Leads gemessen; „AI - Flow“ liefert davon den größten Teil.
- „Ronja - 200€ Facials“ bringt 1 Lead, aber schwächer: CTR 1.87% und CPC 2.76 €.
- Mehrere aktive Ads haben sehr wenig Spend/Impressions — noch keine belastbare Bewertung.

Nächster sinnvoller Schritt:
➡️ Neue Variante von „AI - Flow“ bauen: gleicher Hook „Telefon stört deine Behandlungen?“, aber schnellerer Einstieg mit Szene „Kundin liegt entspannt da, Telefon klingelt“ + 14 Tage kostenlos/200€ Garantie früher zeigen.

Keine Änderungen vorgenommen.
